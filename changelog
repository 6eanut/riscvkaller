2025-11-11
syz-cover

2025-11-10
fix bug and rebase

2025-11-07
1.动态：
    对每个加入到种子库中的种子做分析，
    判断其所包含的系统调用是否触发了arch/riscv下的代码，
    如果是，则把这些系统调用存起来，在种子生成和变异时有意插入这些系统调用
2.种子库复用：
    对每个加入到种子库中的种子，将其所触发的路径保存到本地
3.其他：
    riscv64的qemu启动采用-cpu max